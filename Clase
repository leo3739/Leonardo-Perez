"""
Cree una función que tome un número como su único argumento y devuelva True si es menor o igual a cero; de lo contrario, devuelva False.
"""
def lessThanOrEqualToZero(num):
	if num<=0:
		return True
	else:
		return False
